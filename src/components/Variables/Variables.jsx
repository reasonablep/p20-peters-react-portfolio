const Variables = (props) => {
    const name = "JESTING";
    const sourLevel = 400;

    return (
        <div>
            <h1>{name} about my {sourLevel}</h1>
        </div>
    )

}

export default Variables